version: '3'

services:

  app:
    image: 'app'
    build: './src'

  www:
    image: 'tmvdl/projects:calendar'
    build: './src/www'
    volumes:
      - './src/www/:/app'
    ports:
      - '80:80'
